---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "truenas_snapshot_task_run Action - truenas"
subcategory: "Operations"
description: |-
  Runs a periodic snapshot task immediately (pool.snapshottask.run).
---

# truenas_snapshot_task_run (Action)

Runs a periodic snapshot task immediately (pool.snapshottask.run).

## Example Usage

```terraform
# Run a periodic snapshot task now (Terraform 1.14+).
# id is the numeric task id — reference the managing resource rather than
# hardcoding it (or discover it with `midclt call pool.snapshottask.query`).
action "truenas_snapshot_task_run" "example" {
  config {
    id = truenas_periodic_snapshot_task.hourly.id
  }
}
```

<!-- action schema generated by tfplugindocs -->
## Schema

### Required

- `id` (Number) Periodic snapshot task id (truenas_periodic_snapshot_task.<name>.id).
